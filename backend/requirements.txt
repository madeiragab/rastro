fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
sqlalchemy>=2.0.30,<3.0
alembic>=1.13,<2.0
psycopg[binary]>=3.2,<4.0
geoalchemy2>=0.15,<1.0
shapely>=2.0,<3.0
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0
email-validator>=2.2,<3.0

# --- autenticacao ---
# argon2-cffi: Argon2id, vencedor da Password Hashing Competition e recomendacao
# atual do OWASP para hash de senha. Preferido ao bcrypt por resistir melhor a
# ataque com GPU/ASIC gracas ao custo de memoria.
argon2-cffi>=23.1,<26.0
pyjwt>=2.9,<3.0

# --- notificacao push (Web Push / VAPID) ---
pywebpush>=2.0,<3.0
